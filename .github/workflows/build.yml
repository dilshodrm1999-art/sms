name: Build APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Install SDK packages
        run: |
          yes | sdkmanager --licenses > /dev/null || true
          sdkmanager "platform-tools" "platforms;android-34" "build-tools;34.0.0"

      - name: Grant execute permission for gradlew
        run: chmod +x ./gradlew

      - name: Build debug APK
        run: ./gradlew assembleDebug --stacktrace --no-daemon

      - name: Prepare APK
        run: |
          mkdir -p dist
          cp app/build/outputs/apk/debug/app-debug.apk dist/AmirTemur-BuyukYurishlar-v2.0.apk
          ls -la dist

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: AmirTemur-BuyukYurishlar-APK
          path: dist/AmirTemur-BuyukYurishlar-v2.0.apk
          if-no-files-found: error

      - name: Publish to Release (v2.0)
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v2.0
          name: "Amir Temur: Buyuk Yurishlar — v2.0"
          files: dist/AmirTemur-BuyukYurishlar-v2.0.apk
          body: |
            v2.0 — yangilangan zamonaviy grafika:
            • O'yin boshlanishida Amir Temur siymosi gavdalanadi
            • Janglar real unit ikonalari (otliq, kamonchi, piyoda, zirhli, qamal) + uchuvchi o'qlar bilan
            • Yangilangan zamonaviy UI, animatsiyalar va muvozanat (o'yin yengilroq)

            Tayyor o'rnatiladigan APK (debug imzo bilan). Telefoningizga yuklab,
            "Noma'lum manbalardan o'rnatish"ga ruxsat bering.
            Build raqami: ${{ github.run_number }}
